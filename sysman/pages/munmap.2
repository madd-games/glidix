>NAME

munmap - unmap memory

>SYNOPSIS

	#include <sys/mman.h>
	
	int munmap(void *'addr', size_t 'len');

>DESCRIPTION

This function unmaps memory pages in the range from 'addr' to 'addr+len'. All pages overlapping this region are unmapped, if any. It is not considered an error if this range includes already unmapped pages. 'addr' must be page-aligned. After a successful call tho this function, attempts to access the selected region raise the *SIGSEGV* signal. See [signal.6].

>RETURN VALUE

This function returns '0' on success, or '-1' on error and sets [errno.6] appropriately.

>ERRORS

On error, this function returns '-1' and sets [errno.6] to one of the following values:

\* *EINVAL* - 'addr' is not page-aligned, or the selected region is outside the range allowed for userspace processed (between 2MB and 512GB).

>SEE ALSO

[mmap.2], [mprotect.2], [msync.2]
