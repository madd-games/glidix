%DEPFILE%: %CFILE%
	set -e; rm -f $@; \
	x86_64-elf-gcc -M $(CFLAGS) $< > $@.$$$$; \
	sed 's,%REPLACE%[ :]*,%OBJFILE% $@ : ,g' < $@.$$$$ > $@; \
	rm -f $@.$$$$

%OBJFILE%: %CFILE%
	x86_64-elf-gcc -c $< -o $@ $(CFLAGS)
